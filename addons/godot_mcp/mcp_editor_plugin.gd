@tool
extends EditorPlugin
# MCP Editor Plugin - Empty placeholder for addon registration
# The actual MCP server runs as an autoload at runtime, not as an editor plugin.

func _enter_tree() -> void:
	# Plugin initialization (if needed in the future)
	pass


func _exit_tree() -> void:
	# Plugin cleanup (if needed in the future)
	pass
